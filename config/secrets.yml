production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
  google_client_id: <%= ENV["GOOGLE_CLIENT_ID"] %>
  google_client_secret: <%= ENV["GOOGLE_CLIENT_SECRET"] %>
  askbob_client_id: <%= ENV["ASKBOB_CLIENT_ID"] %>
  askbob_client_secret: <%= ENV["ASKBOB_CLIENT_SECRET"] %>

# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

# Shared secrets are available across all environments.

# shared:
#   api_key: a1B2c3D4e5F6

# Environmental secrets are only available for that specific environment.

development:
 secret_key_base: 390721063f557c4ca16f778cfb1c62613ba8aae9236044cc10fafa658d30003bf7b4fda5bac553e5100698d881469a90fa7115ca82ae594b6a57c9dc140b3f6a
 google_client_id: <%= ENV["GOOGLE_CLIENT_ID"] %>
 google_client_secret: <%= ENV["GOOGLE_CLIENT_SECRET"] %>
 askbob_client_id: <%= ENV["ASKBOB_CLIENT_ID"] %>
 askbob_client_secret: <%= ENV["ASKBOB_CLIENT_SECRET"] %>

test:
 secret_key_base: dc430d9f0a4ac9e1323b10c0bbc77153556ff48c956d69c2a45c04c8305d07982a2961ed03a2cbcec236aa71b2aeda568d83edd3d2166417b2e2bd593b039fbe

# Do not keep production secrets in the unencrypted secrets file.
# Instead, either read values from the environment.
# Or, use `bin/rails secrets:setup` to configure encrypted secrets
# and move the `production:` environment over there.

